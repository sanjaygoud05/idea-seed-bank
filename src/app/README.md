# App shell

Reserved for top-level application concerns (providers, route configuration
helpers, environment bootstrapping) as the project grows.

Today, `src/App.tsx` and `src/main.tsx` remain the entry points.
