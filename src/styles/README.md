# Styles

Global tokens and Tailwind layer overrides live in `src/index.css`. This
directory is reserved for feature-scoped stylesheets (e.g. print styles for
report exports) that shouldn't pollute the global sheet.
